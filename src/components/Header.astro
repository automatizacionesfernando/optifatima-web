---
import { negocio } from '../data/negocio.js';
---

<header class="site-header">
  <div class="container container--wide site-header__inner">
    <a href={import.meta.env.BASE_URL} class="site-header__logo">
      <img
        src={`${import.meta.env.BASE_URL}logo-optifatima.png`}
        alt="Optifatima — Óptica y audiología en Alcobendas"
        style="height: 72px; width: auto;"
      />
    </a>

    <nav class="site-header__nav" aria-label="Navegación principal">
      <a href={`${import.meta.env.BASE_URL}#servicios`}>Servicios</a>
      <a href={`${import.meta.env.BASE_URL}#especialidades`}>Especialidades</a>
      <a href={`${import.meta.env.BASE_URL}#bruno`}>Quién te atiende</a>
      <a href={`${import.meta.env.BASE_URL}marcas`}>Marcas</a>
      <a href={`${import.meta.env.BASE_URL}#contacto`}>Contacto</a>
    </nav>

    <div class="site-header__actions">
      <a href={`tel:${negocio.telefono_internacional}`} class="site-header__phone">
        {negocio.telefono}
      </a>
      <a href={`${import.meta.env.BASE_URL}#contacto`} class="btn btn--primary site-header__cta">
        Pide cita
      </a>
    </div>
  </div>
</header>

<style>
  .site-header {
    position: sticky;
    top: 0;
    z-index: 100;
    background: var(--color-banda-bg);
    border-bottom: 1px solid rgba(0, 0, 0, 0.12);
    backdrop-filter: blur(8px);
  }
  .site-header__inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-6);
    padding: var(--space-4) var(--space-5);
  }
  .site-header__logo {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    font-family: var(--font-display);
    font-size: var(--text-xl);
    font-weight: 500;
    color: var(--color-banda-text);
    letter-spacing: -0.02em;
  }
  .site-header__logo em {
    color: var(--color-banda-text);
    font-style: italic;
  }
  .site-header__nav {
    display: none;
    gap: var(--space-6);
    font-size: var(--text-sm);
    font-weight: 500;
    color: var(--color-banda-text);
  }
  .site-header__nav a { color: var(--color-banda-text); }
  .site-header__nav a:hover { color: var(--color-banda-text); opacity: 0.75; }
  .site-header__actions {
    display: flex;
    align-items: center;
    gap: var(--space-4);
  }
  .site-header__phone {
    display: none;
    font-weight: 600;
    color: var(--color-banda-text);
  }
  .site-header__phone:hover { color: var(--color-banda-text); opacity: 0.75; }
  .site-header__cta {
    padding: var(--space-3) var(--space-5);
    font-size: var(--text-sm);
  }
  @media (min-width: 768px) {
    .site-header__nav    { display: flex; }
    .site-header__phone  { display: inline-block; }
  }
</style>
